import { NextRequest } from "next/server";

const API_BASE = "https://vocespace.com/api/space";
export async function POST(request: NextRequest) {
const url = new URL(API_BASE);
//   url.searchParams.append("space", "create");
//   url.searchParams.append("spaceName", spaceName || username);
//   url.searchParams.append("ownerId", uid);
//   url.searchParams.append("owner", username);
// }
}
